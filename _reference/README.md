# Reference — existing Ren Code app

Drop your existing Ren Code source into this folder, e.g.:

```
_reference/ren-code/
```

This is **reference material** — code I read to understand your existing
Ren Code so I can reimplement its pipeline in this project's stack
(Next.js 15 App Router + Supabase + Astra inference layer).

I will not copy it verbatim. I'll map the architecture end-to-end, write
back my understanding and a port plan, then build our own version.

Source only is enough — you can skip `node_modules` and build output.
